import {placeholder, randomName} from "../../shared/utils";

const spec = {
    name: randomName("task"),
    list_name: randomName("list")
}

describe("", () => {
    it('able to set task important', function () {
        placeholder();
    });

    it('able to expand completed tasks', function () {
        placeholder();
    });
});
